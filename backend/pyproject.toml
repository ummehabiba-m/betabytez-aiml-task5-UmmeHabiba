[project]
name = "betabytez-task5-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]",
    "pandas",
    "matplotlib",
    "seaborn",
    "groq",
    "python-multipart",
    "pydantic",
]

[tool.fastapi]
entrypoint = "main:app"